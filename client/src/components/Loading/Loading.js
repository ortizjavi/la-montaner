import React from 'react';
import BeatLoader from "react-spinners/BeatLoader";

const Loading = () => {
    return (
        <div>
            <BeatLoader size={30}/>
        </div>
    );
};

export default Loading;